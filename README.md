# Rails environment with rbenv, ansible

### vagrant with provisioning

```sh
vagrant up
```

### ansible(virtual machine already exists)

```sh
ansible-playbook build-server.yml -i hosts-vagrant 
```


